---
title: Connect
description: Wire up your coding agent to a Context Graph in one command
---

## Setup

The CLI tool `rippletide-code` handles all the wiring. One command connects your coding agent to a Context Graph:

```bash
npx rippletide-code@latest connect
```

This does two things:

1. Creates `.codex/config.toml` — configures the MCP connection for Codex
2. Creates `CLAUDE.md` / `AGENTS.md` — injects system prompt instructions so the agent knows how to use the graph tools

### What gets generated

After running `connect`, your project root will contain:

```
your-project/
├── .codex/config.toml     # MCP server config (Codex)
├── CLAUDE.md              # Agent instructions (auto-generated)
└── AGENTS.md              # Agent instructions (auto-generated)
```


---
title: Get Started
description: Evaluate your first AI agent in 5 minutes
---

## Quick Start

Get up and running with Rippletide in three steps: install the CLI, point it at your agent, and see evaluation results.

### 1. Install the CLI

```bash
npm install -g rippletide
```

Or run directly with npx:

```bash
npx rippletide
```

### 2. Prepare Your Test Data

Create a `qanda.json` file with the questions and expected answers you want to test:

```json
[
  {
    "question": "What is your return policy?",
    "answer": "We offer a 30-day return policy for most items."
  },
  {
    "question": "How can I track my order?",
    "answer": "Log into your account and go to the My Orders section."
  }
]
```

### 3. Run Your First Evaluation

```bash
rippletide
```

The CLI will prompt you for your agent's endpoint URL, then automatically:
- Send each question to your agent
- Compare responses against expected answers
- Detect hallucinations
- Display a pass/fail summary with a link to the detailed dashboard

## What's Next?

Now that you've evaluated your agent, explore the three pillars of Rippletide:

<CardGroup cols={3}>
  <Card title="Eval" icon="chart-line" href="/docs/evaluation_overview">
    Learn about hallucination detection, CLI options, and the Python SDK
  </Card>

  <Card title="Context Graph" icon="diagram-project" href="/docs/mcp/overview">
    Give your agents persistent memory with a knowledge graph
  </Card>

  <Card title="Orchestration" icon="wand-magic-sparkles" href="/docs/hypergraph_overview">
    Build deterministic agents with less than 1% hallucination rate
  </Card>
</CardGroup>

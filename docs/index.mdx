---
title: "What is Rippletide?"
description: "Start here: what Rippletide does, and how teams adopt it safely."
---

Rippletide turns business context into enforceable rules for AI agents.

Connect your agent or MCP server, add rules, and enforce them before any tool call, API call, data query, or other action your agent can take.

This helps ensure your agent follows those instructions and prevents costly mistakes before an irreversible action.

Teams use Rippletide with coding agents, customer-support agents, and finance agents to prevent unapproved refunds, sensitive customer-data access, or production API calls without the required evidence or approval.


{/* start-options:start */}

## Choose how to start

- [**Set up with a coding agent**](/docs/connect-agent): give Codex, Claude Code, or another coding agent the setup prompt.
- [**Set up with the CLI**](/docs/cli): connect and manage Rippletide from your repository.
- [**Set up in the app**](https://app.rippletide.com): connect through the Rippletide UI.

{/* start-options:end */}

## The three questions Rippletide answers

1. **What is my harness made of?** Rippletide shows you what the model sees: prompts, tools, and third parties.
2. **What actions are allowed?** Rippletide compiles rules into versioned, auditable policy releases. Rippletide suggests rules that are relevant to each agent, or lets you define them manually.
3. **What happens at runtime?** Before an important action, Rippletide evaluates your rules and returns a decision with a full audit trail: allow, escalate, or block.

## Monitor first, enforce deliberately

Rippletide is built for a careful rollout. Nothing is ever blocked until you explicitly say so.

1. **Connect** your agent and inspect its harness: prompts, tools, data connections etc.
2. **Define your rules** against the actions your agent can take.
3. **Observe** real runtime behavior. A rule in observe mode reports *would block*; your agent is never interrupted.
4. **Enforce** once you trust a rule. Move that release to enforce mode and matching actions are actually prevented, with a receipt of what happened.

Your agent stays exactly where it is today: your code, your infrastructure, your credentials. Rippletide never hosts, launches, or rewrites it.

## Where to go next

- [Quickstart](/docs/quickstart): connect your first agent in minutes.
- [Write rules](/docs/write-rules): turn plain-English policies into enforceable releases.
